include 'skippy-core'
include 'skippy-maven'

if ( ! file('sandbox').text.toBoolean()) {
    include 'skippy-gradle'
} else {
    includeBuild 'skippy-gradle'
    include 'skippy-gradle-sandbox'
}
include 'skippy-junit4'
include 'skippy-junit5'